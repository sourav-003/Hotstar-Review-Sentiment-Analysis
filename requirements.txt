# Core runtime for the Gradio app
gradio>=4.0.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
# Use tensorflow-cpu on servers without GPU. If you have GPU, replace with appropriate tensorflow package.
tensorflow>=2.12,<3.0
joblib>=1.3
nltk>=3.8


# Libraries used elsewhere in the notebook (training & EDA)
xgboost>=1.7
wordcloud>=1.9
matplotlib>=3.7
